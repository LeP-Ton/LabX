# 初始化 LabX 项目文档

## 背景与目标

- 仓库根目录仅存在 `.git`，缺少项目认知与变更索引。
- 建立最小文档骨架，同时避免对 LabX 的产品定位和技术实现作无依据推断。

## 约束与原则

- 所有项目认知均以当前可验证的仓库状态为准。
- 未知的产品目标、技术栈、架构与运行方式明确标记为待确认。
- 后续先读取根索引，再按需读取单个变更文档。

## 阶段与 TODO

- [x] 创建根目录 `AGENTS.md`。
- [x] 创建 `CLAUDE.md` 兼容入口。
- [x] 创建 `.agentdocs/index.md` 根索引。
- [x] 创建首份初始化变更文档。
- [ ] 用户补充 LabX 定义后，更新整体项目认知。

## 关键风险

- “LabX”存在同名项目或产品，仅凭目录名推断会造成错误定位。
- 当前没有源码，因此无法判断实际技术栈、架构和运行方式。

## 当前进展

- 项目文档骨架已建立。
- 产品定义仍待用户补充。

## 代码变更

- 本次未变更业务代码。
- 新增项目治理文档，内容变更如下：

```diff
++ AGENTS.md
+# LabX 项目认知
+
+## 基本信息
+
+- 项目名称：LabX。
+- 当前状态：空白 Git 仓库，尚未提供产品定义、源码或依赖配置。
+- 产品目标：待用户补充后更新。
+
+## 技术选型与核心架构
+
+- 技术栈：尚未确定。
+- 核心架构：尚未建立。
+- 在产品目标与约束明确前，不预设框架、目录结构或基础设施。
+
+## 运行方式
+
+- 当前没有可运行程序。
+- 后续引入运行命令时，应在本文件记录最小启动、测试与构建方式。
+
+## 协作与文档约定
+
+- 与用户的沟通、项目文档和代码注释使用中文；必要的英文专业名词首次出现时附中文说明。
+- 每次代码变更均在 `.agentdocs/workflow/` 新建带 `yyyyMMddHHmmss` 时间前缀的 Markdown 变更文档。
+- 每份变更文档应包含背景、目标、约束、进展、风险、完整 diff 格式的代码变更和测试用例。
+- 仅在整体定位、技术选型、核心架构或运行方式发生变化时更新本文件；具体需求迭代记录在 `.agentdocs/`。
+- `CLAUDE.md` 仅引用本文件，避免维护两份项目认知。
+
+## 检索方法
+
+1. 首先读取 `.agentdocs/index.md`，了解当前有效文档及其读取场景。
+2. 根据索引按需读取 `.agentdocs/workflow/` 中的单个变更文档，不直接全量检索该目录。
+3. 再使用 `rg` 检索源码、配置与测试；如果环境没有 `rg`，再使用等价工具。
++ CLAUDE.md
+@AGENTS.md
++ .agentdocs/index.md
+# LabX 文档索引
+
+## 项目认知
+
+`../AGENTS.md` - 记录项目整体定位、技术选型、核心架构、运行方式与检索方法；开始项目工作时优先读取。
+
+## 当前变更文档
+
+`workflow/20260830143747-initialize-project-docs.md` - 初始化项目认知、兼容入口与文档索引；需要了解仓库初始状态时读取。
+
+## 关键记忆
+
+- 仓库初始化时只有 `.git`，尚无源码、产品定义、技术栈或运行命令。
+- “LabX”的具体产品含义仍需用户确认，不能仅根据目录名推断。
```

## 测试用例

### TC-001 文档入口完整

- 类型：文档检查
- 优先级：高
- 前置条件：位于项目根目录。
- 操作步骤：检查 `AGENTS.md`、`CLAUDE.md`、`.agentdocs/index.md` 和本变更文档是否存在且可读取。
- 预期结果：四个文件存在，索引路径有效，`CLAUDE.md` 正确引用 `AGENTS.md`。
- 是否通过：已通过；四个文件均存在且可读取，索引引用有效。
